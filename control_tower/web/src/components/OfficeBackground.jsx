// The pixel office now renders its own room (wood floor + navy walls +
// HQ board) inline inside PixelOffice, so this component is intentionally
// empty. Kept as a stub so legacy imports do not crash.
export default function OfficeBackground() {
  return null;
}
